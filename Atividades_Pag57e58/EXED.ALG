Algoritmo "Exed"


Var

tp, vl, cons, dt : real

Inicio

escreva ("Em minutos, qual será o tempo da viagem? ")
        leia (tp)
escreva ("Qual a velocidade média no percurso? ")
        leia (vl)

dt <- (vl*tp)

cons <- (dt/12)

escreva ("O consumo aproximado, em litros, para este percurso será de: ", cons)


Fimalgoritmo